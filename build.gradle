plugins {
	id 'java'
}

libsDirName = '../Plugins'

dependencies {
	implementation files("${aoiLocation}")
}

sourceSets {
    main {
        java {
            srcDirs = ['src/']
        }
        resources {
            srcDirs = ['src/']
        }
    }
}
